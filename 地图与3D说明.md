# 地图扩展与 3D（v4）

在“基层地图”上方点击“扩展地图”，自定义宽高。初始为 18×14 格，可逐步向东、向南扩展到 64×64 格（4096 格）。当前不支持缩小或无限地图，避免删除已有城市与过大的渲染开销。乡镇岗位可扩展，县级以上联系点依然只读。

扩展规划范围不扣款、不增加人口或收入。实际建路、建房仍消耗财政和年度预算。原有建筑坐标不变，项目申请和专项债地块索引随新宽度同步更新；河流在新增南部区域延续。

默认显示 3D：拖动旋转和调整俯仰、滚轮或加减按钮缩放、Shift 拖动平移，全景复位恢复镜头。点击模型或地面可查看、建设或拆除，规则与平面完全一致。触屏可拖动旋转并使用缩放按钮；键盘精确建设可切换平面，用方向键与 Enter 操作。

建筑采用本地生成的低多边形模型，含屋顶、窗户、烟囱、树木、水池、医院标记、施工立柱等。使用三维坐标和正交投影在 Canvas 绘制，无外部依赖，可离线运行。当前为轻量城市沙盘，没有写实材质、行人交通动画或起伏地形。

v1–v3 存档按原 18×14 格迁移至 v4，保留财政、城市和职业生涯。v4 存档保存实际宽高，不应再用旧版代码打开。

执行 `npm test`：39 项测试，含扩图坐标与账目保留、扩图后的项目审批、新区域建设、桥梁拆除、权限和尺寸拒绝、64×64 结算及存档恢复，以及原有治理与职业全流程。
